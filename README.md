# Task Service

Сервис для управления задачами с HTTP API на Go.


# Настройка периодичности задач

Периодичность задач реализована с учетом сценариев по типам задач. ТЗ по этой задаче доработано таким образом, что
- если задача ставится на часы, которые еще не прошли, обновление задачи выполняется тем же днем
- для каждого типа периодичности указывается время в формате часы:минуты, когда задача должна быть обновлена. Таким образом, вроме периодичности нужно указывать час и минуту, когда будет выполнено обновление задачи

Доработано ТЗ в части ввода ограничений на вход:
- для ежедневных задач реализованы интервалы, от 1 (этот или следующий день) до 365 (через год)
- для ежемесячных задач указывается число месяца (от 1 до 31, в начальном ТЗ не учтено 31 число). Если в месяце меньше 30 или 31 числа, указанных в запросе, будет выбрано последнее число месяца
- задачи на конкретные даты ставятся в формате YYYY-MM-DD

Доработано ТЗ в части планирования обновления задачи. В текущей редакции работает отдельный планировщик, который каждую минуту проверяет наличие задач для обновления статуса. Для каждой такой задачи запускается отдельный обработчик, максимальное количество обработчиков для одной итерации ограничено и равно 5.

Предполагается, что обновление задачи - это изменение ее статуса на "new". В настоящей редакции нет учета того, что задача полностью закрыта, и не требуется изменение ее статуса, если настроена периодичность.

Для полного тестирования функционала необходимо добавить тесты БД, интеграционные тесты, включающие
- тесты всех SQL запросов
- тесты отката миграций (в текущей версии пока нет down-миграций)
- тесты работы планировщика в штатном режиме, в случае сбоев в работе планирования задач на день, неделю и тд. 
- тесты API

## Требования

- Go `1.23+`
- Docker и Docker Compose

## Быстрый запуск через Docker Compose

```bash
docker compose up --build
```

После запуска сервис будет доступен по адресу `http://localhost:8080`.

Если `postgres` уже запускался ранее со старой схемой, пересоздай volume:

```bash
docker compose down -v
docker compose up --build
```

Причина в том, что SQL-файл из `migrations/0001_create_tasks.up.sql` монтируется в `docker-entrypoint-initdb.d` и применяется только при инициализации пустого data volume.

## Swagger

Swagger UI:

```text
http://localhost:8080/swagger/
```

OpenAPI JSON:

```text
http://localhost:8080/swagger/openapi.json
```

## API

Базовый префикс API:

```text
/api/v1
```

Основные маршруты:

- `POST /api/v1/tasks`
- `GET /api/v1/tasks`
- `GET /api/v1/tasks/{id}`
- `PUT /api/v1/tasks/{id}`
- `DELETE /api/v1/tasks/{id}`

## Запуск тестов

```bash
docker compose --profile test run --rm test go test ./...
```

